# Prometheus configuration for JARVIS-X
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'jarvis-x'
    static_configs:
      - targets: ['host.docker.internal:8000']
        labels:
          instance: 'jarvis-trading-system'
